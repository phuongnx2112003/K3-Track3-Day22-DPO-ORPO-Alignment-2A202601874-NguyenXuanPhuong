seed: 42
paths:
  model_name: distilgpt2
  train_data: data/processed/train.jsonl
  validation_data: data/processed/validation.jsonl
  output_dir: outputs
training:
  method: dpo  # dpo
  beta: 0.1
  lambda_orpo: 0.1
  max_length: 512
  batch_size: 2
evaluation:
  regression_prompts: docs/regression_prompts.md
